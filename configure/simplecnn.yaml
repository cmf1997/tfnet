name: SimpleCNN
path: models

model:
  emb_size: 6 # for simplecnn
  conv_size: [5, 9, 13, 17, 21] # for simplecnn
  conv_num: [128, 128, 128, 128, 128] # for simplecnn
  conv_off: [8, 6, 4, 2, 0] # for simplecnn
  linear_size: [256, 512] # for simplecnn
  full_size: [26624, 1024] # for simple cnn

  dropouts: [0.2, 0.5]

  DNA_pad: 10

padding:
  target_len: 200
  DNA_len: 1024
  DNA_pad: 10
  tf_len: 39
  DNA_N: False

train:
  batch_size: 64
  num_epochs: 5
  opt_params:
    #optimizer_cls: Adadelta
    #lr: 0.05
    weight_decay: 1e-4
    optimizer_cls: Adam
    lr: 0.001

valid:
  batch_size: 64

test:
  batch_size: 64
  
classweights: True